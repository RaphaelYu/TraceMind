[project]
name = "tm-example-exporter"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = []

[project.entry-points."trace_mind.exporters"]
example = "example_exporter.exporter:ExampleExporter"
